(function(){
  function esc(value){return String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
  function checks(labels,selected){return labels.map((label,index)=>`<span class="check">${selected===(['ok','ng','ask'][index])?'☑':'□'} ${label}</span>`).join('　');}
  function markup(sheet,template,blank=false){const value=v=>blank?'':(v||'');return `<div class="sheet-top"><img class="nagare-logo" src="assets/nagare-key/logo.png" alt="ながれキー"><span class="tenkey-mark">#天キーVol.12</span><h2 class="sheet-name">${esc(value(sheet.keyboardName))}</h2><div class="sheet-exhibitor"><span class="exhibitor-label">EXHIBITOR / HANDLE</span><span class="exhibitor-value">${esc(value(sheet.exhibitor))}</span></div></div><div class="sheet-meta"><div class="meta-item"><span class="meta-label">KEY SWITCH</span><span class="meta-value">${esc(value(sheet.switchName))}</span></div><div class="meta-item"><span class="meta-label">KEYCAPS</span><span class="meta-value">${esc(value(sheet.keycaps))}</span></div></div><div class="sheet-description">${esc(value(sheet.description))}</div><div class="permission-grid"><div><span class="permission-label">試し打ち</span><span class="permission">${checks(['OK','NG','声かけて'],sheet.typing)}</span></div><div><span class="permission-label">撮影・SNS投稿</span><span class="permission">${checks(['OK','NG'],sheet.photo)}</span></div></div><div class="sheet-footer"></div>`;}
  function fitDescription(card){const element=card.querySelector('.sheet-description');if(!element)return 'normal';const levels=['normal','small','smaller','minimum'];element.classList.remove(...levels);let chosen='minimum';for(const level of levels){element.classList.add(level);if(element.scrollHeight<=element.clientHeight+1){chosen=level;break;}}element.classList.add(chosen);return chosen;}
  window.SheetCard={markup,fitDescription};
})();
